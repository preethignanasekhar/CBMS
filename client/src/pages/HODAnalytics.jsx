import React from 'react';
import GraphicalDashboard from './GraphicalDashboard';

const HODAnalytics = () => {
    // Reuse GraphicalDashboard but maybe with more department-specific focus if needed
    // For now, it provides the required charts
    return <GraphicalDashboard />;
};

export default HODAnalytics;
